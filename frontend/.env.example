# Base URL of the ResQAI FastAPI backend (no trailing slash needed).
# Variable NAMES only -- never put secrets in VITE_ variables: they are
# embedded in the public JavaScript bundle.
VITE_API_BASE_URL=http://127.0.0.1:8000
